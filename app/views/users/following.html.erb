<% @users.each do |user| %>
<table>
	<tr>
		<td><%= attachment_image_tag user, :profile_image, :fill, 100,100 ,fallback: "noimage.png",size:'100x100' %></td>
		<td> <%= user.name %> </td>
		<td> <%= link_to "ホーム画面", user_path(user) %> </td>
	</tr>
</table>
<% end %>